@inject IStringLocalizer<SharedResource> LS
<AddEditModal RequestModel="NewsPostModel" SaveFunc="@createFunc" @ref="addeditModel" IsCreate="IsCreate" Title="@Caption">
    <MudGrid>

        <MudItem xs="12" md="12">
            <MudTextField T="string" Label="@LS["Caption"]" Variant="Variant.Outlined" @bind-Value="context.Title" For="@(()=>context.Title )"
                          InputType="InputType.Text" />
            @*<MudTextField T="string" Label="@LS["Text"]" Lines="5" Variant="Variant.Outlined" @bind-Value="context.Body" For="@(()=>context.Body)"
                          InputType="InputType.Text" />*@
        </MudItem>
        <MudItem xs="12" md="12" >
            <InputRichText @bind-Value="context.Body" UrlToPostImage="http://localhost:44301/api/qaiser/Upload" style="width:100%;height:200px;" />
        </MudItem>
        
       



    </MudGrid>
</AddEditModal>

@code {
     
    [Parameter]
    public bool IsCreate { get; set; }
    [Parameter]
    public string Caption { get; set; } = default!;
    [Parameter]
    [EditorRequired]
    public UpdateNewsPostRequest NewsPostModel { get; set; } = default!;
    private AddEditModal<UpdateNewsPostRequest> addeditModel = default!;
    private async Task createFunc(NewsPostDto prod)
    {
       
        
        //await NativesClient.CreateAsync(prod.Adapt<CreateNativeRequest>());
        // prod.ImageInBytes = string.Empty;
    }

    

     

}
